name: CI

on:
  pull_request:
    types: [closed]
    branches:
      - main

permissions: read-all

jobs:
  test:
    if: ${{ github.event.pull_request.merged == true }}
    name: 3 - Test & Build
    timeout-minutes: 20
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
    outputs:
      started_at: ${{ steps.start.outputs.started_at }}
      finished_at: ${{ steps.finished.outputs.finished_at }}
    steps:
      - id: start
        run: |
          echo "started_at=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" >> "$GITHUB_OUTPUT"
      - uses: actions/github-script@v7
        id: create
        with:
          script: |
            const MARKER = '<!-- ci-status-marker -->';
            const body = `
            ${MARKER}
            ### 🚧 Post-merge pipeline is running…
            * **Test & Build**: 🔸 pending
            * **Report Links Comment**: 🔸 pending
            * **Release**: 🔸 pending

            _Workflow_: \`${context.workflow}\`
            _Run_: ${context.runNumber}
            `;
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              per_page: 100
            });
            const existing = comments.find(c =>
              typeof c.body === 'string' && c.body.includes(MARKER)
            );
            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body
              });
            }

      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "pnpm"
      - run: pnpm install --frozen-lockfile
      - run: pnpm lint
      - run: pnpm tsc --noEmit
      - run: pnpm coverage
      - run: pnpm build
      - uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./__local__/coverage
          destination_dir: reports/stable/coverage
      - uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./__local__/bundle
          destination_dir: reports/stable/bundle
      - name: Update status for Test & Build
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const MARKER = '<!-- ci-status-marker -->';
            const finished = new Date().toISOString().replace('T',' ').replace('Z',' UTC');
            const icon = '${{ job.status }}' === 'success' ? '✅' :
                        ('${{ job.status }}' === 'failure' ? '❌' : '⚠️');

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              per_page: 100
            });
            const existing = comments.find(c => typeof c.body === 'string' && c.body.includes(MARKER));
            if (!existing) return;

            const updated = existing.body
              .replace('**Test & Build**: 🔸 pending', `**Test & Build**: ${icon} finished at ${finished}`);

            await github.rest.issues.updateComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: existing.id,
              body: updated
            });
      - id: finished
        run: |
          echo "finished_at=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" >> "$GITHUB_OUTPUT"

  comment:
    name: 4 - Report Links Comment
    timeout-minutes: 1
    runs-on: ubuntu-latest
    needs: test
    permissions:
      issues: write
      pull-requests: write
    outputs:
      finished_at: ${{ steps.finished.outputs.finished_at }}
    steps:
      - uses: actions/github-script@v7
        with:
          script: |
            const baseUrl = `https://${context.repo.owner}.github.io/${context.repo.repo}/reports/stable`
            const coverageUrl = `${baseUrl}/coverage/index.html`
            const bundleUrl = `${baseUrl}/bundle/sonda_0.html`

            const body = `
            📝 **Reports for commit \`latest-stable\`**

            - [Coverage Report](${coverageUrl})
            - [Bundle Analysis](${bundleUrl})
            `

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            })

            const existing = comments.find(c => c.user.type === "Bot" && c.body.includes("Reports for commit"))
            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body
              })
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body
              })
            }
      - name: Update status for Report Links Comment
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const MARKER = '<!-- ci-status-marker -->';
            const finished = new Date().toISOString().replace('T',' ').replace('Z',' UTC');
            const icon = '${{ job.status }}' === 'success' ? '✅' :
                        ('${{ job.status }}' === 'failure' ? '❌' : '⚠️');
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              per_page: 100
            });
            const existing = comments.find(c => typeof c.body === 'string' && c.body.includes(MARKER));
            if (!existing) return;

            const updated = existing.body
              .replace('**Report Links Comment**: 🔸 pending', `**Report Links Comment**: ${icon} finished at ${finished}`);

            await github.rest.issues.updateComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: existing.id,
              body: updated
            });
      - id: finished
        run: |
          echo "finished_at=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" >> "$GITHUB_OUTPUT"

  release:
    name: 5 - Release
    runs-on: ubuntu-latest
    needs: test
    permissions:
      contents: write
    outputs:
      version: ${{ steps.version.outputs.version }}
      finished_at: ${{ steps.finished.outputs.finished_at }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - id: version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Create release branch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -b release/v${{ steps.version.outputs.version }}
          git push origin release/v${{ steps.version.outputs.version }}

      - name: Create tag
        run: |
          git tag v${{ steps.version.outputs.version }}
          git push origin v${{ steps.version.outputs.version }}
      - name: Update status for Release
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const MARKER = '<!-- ci-status-marker -->';
            const finished = new Date().toISOString().replace('T',' ').replace('Z',' UTC');
            const icon = '${{ job.status }}' === 'success' ? '✅' :
                        ('${{ job.status }}' === 'failure' ? '❌' : '⚠️');
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              per_page: 100
            });
            const existing = comments.find(c => typeof c.body === 'string' && c.body.includes(MARKER));
            if (!existing) return;

            const updated = existing.body
              .replace('**Release**: 🔸 pending', `**Release**: ${icon} finished at ${finished}`);

            await github.rest.issues.updateComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: existing.id,
              body: updated
            });
      - id: finished
        run: |
          echo "finished_at=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" >> "$GITHUB_OUTPUT"

  success:
    name: 6 - Success
    runs-on: ubuntu-latest
    needs: [test, comment, release]
    permissions:
      issues: write
      pull-requests: write
    steps:
      - uses: actions/github-script@v7
        with:
          script: |
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const prNumber = context.issue.number;

            const version = '${{ needs.release.outputs.version }}';
            const tag = `v${version}`;
            const releaseBranch = `release/v${version}`;

            const baseUrl = `https://${owner}.github.io/${repo}/reports/stable`;
            const coverageUrl = `${baseUrl}/coverage/index.html`;
            const bundleUrl = `${baseUrl}/bundle/sonda_0.html`;

            const branchUrl = `https://github.com/${owner}/${repo}/tree/${releaseBranch}`;
            const tagUrl = `https://github.com/${owner}/${repo}/tree/${tag}`;
            const commitSha = context.payload.pull_request?.merge_commit_sha ?? context.sha;
            const commitUrl = `https://github.com/${owner}/${repo}/commit/${commitSha}`;

            const badge = (label, message, color) =>
              `![label](https://img.shields.io/badge/${label}-${message}-${color}?style=for-the-badge)`;

            const badges = [
              `${badge('release', tag, 'darkcyan')}`,
              `${badge('branch', releaseBranch, 'informational')}`,
              `${badge('commit', commitSha.slice(0,7),'rebeccapurple')}`,
              `${badge('status', 'success', 'success')}`
            ].join(' ');

            const timestamp = new Date().toLocaleString('en-US', { timeZone: 'UTC', hour12: false }) + ' UTC';

            const MARKER = '<!-- release-comment-marker -->';
            const body = `
            ${MARKER}
            ## 🚀 **Release \`${tag}\` is live!**  🎉
            ${badges}

            > This pull request was merged and promoted to **\`${releaseBranch}\`** with tag **\`${tag}\`**.

            **Quick links**
            - 📦 **Release branch:** \`${releaseBranch}\`
            - 🏷️ **Tag:** \`${tag}\`
            - 🧪 **Coverage report:** [Open](${coverageUrl})
            - 📊 **Bundle analysis:** [Open](${bundleUrl})
            - 🔗 **Merge commit:** \`${commitSha.slice(0,7)}\`

            <details>
            <summary>ℹ️ Checks</summary>

            - **Test & Build**: ✅ passed at ${{ needs.test.outputs.finished_at }}
            - **Report Links Comment**: ✅ passed at ${{ needs.comment.outputs.finished_at }}
            - **Release**: ✅ passed at ${{ needs.comment.outputs.finished_at }}

            </details>

            <details>
            <summary>ℹ️ Details</summary>

            - Triggered by: @\${context.actor}
            - Workflow: \`${context.workflow}\`
            - Run: [${context.runNumber}](${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}) _If you re-run the workflow, this comment will update automatically._ ✨
            `;
            </details>

            const { data: comments } = await github.rest.issues.listComments({
              owner, repo, issue_number: prNumber, per_page: 100
            });

            const existing = comments.find(c =>
              c.user?.type === 'Bot' &&
              typeof c.body === 'string' &&
              c.body.includes(MARKER)
            );

            if (existing) {
              await github.rest.issues.updateComment({
                owner, repo, comment_id: existing.id, body
              });
            } else {
              await github.rest.issues.createComment({
                owner, repo, issue_number: prNumber, body
              });
            }
