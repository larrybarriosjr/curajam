import { DEFAULT_THEME, type MantineThemeOverride } from "@mantine/core"

export const theme: MantineThemeOverride = {
	headings: {
		fontFamily: `Livvic, ${DEFAULT_THEME.headings.fontFamily}`,
	},
}
